describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});

describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});

describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});

describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});

describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});

describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});

describe('some test', () => {
  it('can do something', () => {
    const x = 1 + 1;
  })
});
